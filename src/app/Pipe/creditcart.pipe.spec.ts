import { CreditcartPipe } from './creditcart.pipe';

describe('CreditcartPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditcartPipe();
    expect(pipe).toBeTruthy();
  });
});
